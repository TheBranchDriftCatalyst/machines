all:
  hosts:

    localhost:
      ansible_connection: local
      ansible_host: 127.0.0.1
      # ansible_become: true

    truenas00:
      ansible_host: 192.168.1.200

    nuc00:
      ansible_host: 192.168.1.30
      ansible_ssh_pass: "{{ lookup('env', 'CATALYST_SSH_PASS') }}"
      ansible_become: true
      ansible_become_pass: "{{ lookup('env', 'CATALYST_BECOME_PASS') }}"

    syno00:
      ansible_host: 192.168.1.192

  children:
    homelab:
      hosts:
        truenas00:
        syno00:
        nuc00:

          # Example env vars
          # vars:
          #   some_api_key: "{{ lookup('env', 'MY_API_KEY') }}"
