# Molecule testing dependencies
molecule>=6.0.0
molecule-plugins[docker]>=23.0.0
molecule-plugins[lima]>=1.0.0
ansible>=8.0.0
ansible-lint>=6.0.0
yamllint>=1.20.0
pytest>=7.0.0
pytest-ansible>=4.0.0

# Docker support
docker>=6.0.0

# Lima support for macOS VMs
lima-vm>=0.18.0